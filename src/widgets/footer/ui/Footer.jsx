import { Layout } from "antd";

const footerStyle = {
  height: 60,
  backgroundColor: "red",
};
export const Footer = () => {
  const { Footer } = Layout;
  return <Footer style={footerStyle}>Footer</Footer>;
};
